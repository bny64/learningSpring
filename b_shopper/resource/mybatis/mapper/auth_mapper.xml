<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bny.dao.UserDao">
	<insert id="insertUser" parameterType="com.bny.dto.User">
		INSERT INTO user(id, password, name, email, address, userType, userClass, joinType)
		VALUES(#{id}, #{password}, #{name}, #{email}, #{address}, #{userType}, #{userClass}, #{joinType})
	</insert>
</mapper>
 